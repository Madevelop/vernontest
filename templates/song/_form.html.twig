{{ form_start(form) }}

    {{ form_row(form.title) }}
    {{ form_row(form.groupe) }}
    {{ form_row(form.album) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
